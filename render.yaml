services:
  - type: web
    name: daily-schedule-reminder
    env: static
    buildCommand: npm run build
    publishDir: dist
    envVars:
      - key: NODE_VERSION
        value: 20
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
